/** Host loader entry for the browser draft catalog implementation exported from `./client`. */

/** Host plugin body — no Host behavior; the catalog seam lives in dsh-host-composer-catalog. */
export function apply(): void {}
